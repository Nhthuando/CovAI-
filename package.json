{
  "name": "covai-root",
  "private": true,
  "scripts": {
    "dev": "npx concurrently --names \"client,server\" --prefix-colors \"blue,green\" \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "install:all": "npm install --prefix client && npm install --prefix server"
  }
}